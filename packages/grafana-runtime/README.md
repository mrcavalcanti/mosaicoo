# Mosaicoo Runtime library

> **@grafana/runtime is currently in BETA**.

This package allows access to mosaicoo services. It requires Mosaicoo to be running already and the functions to be imported as externals.
